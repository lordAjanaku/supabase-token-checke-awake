name: Keep Supabase Awake

on:
  schedule:
    - cron: '* * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase Health Function
        run: curl https://rwdyanuhxnmbvuhupzbc.supabase.co/functions/v1/health
